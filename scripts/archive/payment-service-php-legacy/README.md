# Legacy PHP payment service (archived)

NganLuong `NL_Checkoutv3` integration — **not used in production**.

Production payments use **SePay** via `server/lib/sepay.ts` and `POST /api/payment/*`.

Kept for reference only. Safe to delete this folder if not needed.
