# Legacy Docker Compose deploy — disabled (use deploy.yml: PM2 + docker-compose.vps.yml).
# Manual trigger only if you intentionally need docker-compose.production.yml.

name: Deploy to Contabo VPS (Docker — legacy)

on:
  workflow_dispatch:

jobs:
  deploy-legacy-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow is legacy. Use 'Deploy to Contabo VPS' (deploy.yml) instead."
          exit 1
